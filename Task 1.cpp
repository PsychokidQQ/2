#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    //Приклад 1
    float x, y, Y;
    
    if(x <= 0)
        Y = pow(x, 2) - sin(y);
    else if(x > 0)
        Y = sqrt(x) + cos(y);
    
    return 0;
}